#include "main.h"

/**
 * main - is a program that prints its name, followed by a new line.
 *
 * @ac: is the argument count
 * @av: is the argument input
 *
 * Return: 0
 */

int main(int ac, char **av)
{
	printf("%s\n", av[0]);
	return (0);
}
